// Update keys state'

up = keyboard_check_pressed(def_key[key.up]);
up_hold = keyboard_check(def_key[key.up]);

down = keyboard_check_pressed(def_key[key.down]);
down_hold = keyboard_check(def_key[key.down]);

left = keyboard_check_pressed(def_key[key.left]);
left_hold = keyboard_check(def_key[key.left]);

right = keyboard_check_pressed(def_key[key.right]);
right_hold = keyboard_check(def_key[key.right]);

jump = keyboard_check_pressed(def_key[key.jump]);
jump_hold = keyboard_check(def_key[key.jump]);